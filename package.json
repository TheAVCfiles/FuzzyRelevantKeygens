{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm --filter @workspace/api-spec run check && pnpm run typecheck:libs && API_CONTRACTS_CHECKED=1 pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "validate:release": "pnpm run typecheck && pnpm --filter @workspace/api-server test && pnpm --filter @workspace/autography test && pnpm --filter @workspace/api-server build && PORT=25100 BASE_PATH=/ pnpm --filter @workspace/autography build",
    "audit:release": "pnpm --filter @workspace/scripts run release:audit --",
    "test:release-audit": "pnpm --filter @workspace/scripts test"
  },
  "private": true,
  "dependencies": {
    "@google/adk": "2.0.0",
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
